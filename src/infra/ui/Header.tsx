export function Header() {
    return (
        <header className="mb-4">
            <h1 className="text-xl font-semibold">Mini React Lab</h1>
            <p className="text-sm text-default-500">
                Work through the exercises, then we’ll build a Todo app.
            </p>
        </header>
    )
}
