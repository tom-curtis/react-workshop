export default function Ex3EventRegistrationForm() {
    /**
     * TODOs:
     *
     * - Build a controlled form with inputs for:
     *   name, email, ticketType, and a “terms accepted” checkbox.
     *   (See: Forms & Validation.)
     *
     * - Track all fields in state using useState.
     *
     * - Implement simple validation (min length, @ in email, etc.).
     *
     * - Conditionally show validation messages near inputs.
     *
     * - Disable submit until all validation passes.
     *
     * - On submit, prevent default and show a confirmation summary
     *   (e.g. “Thanks {name}, you registered for a {ticketType} ticket.”)
     */
    return <form>{/* TODO */}</form>
}
