export default function Ex6NotificationCenter() {
    /**
     * TODOs:
     *
     * - Maintain an array of notifications (id, message, type, createdAt).
     *
     * - Render notifications using .map() with styling based on type.
     *   (See: Lists & Conditional Rendering.)
     *
     * - Add a button that creates and pushes a new notification.
     *
     * - Allow manual dismissal of notifications.
     *
     * - (Stretch) Use useEffect + setTimeout to auto-dismiss
     *   notifications after a few seconds.
     *   (See: Side Effects.)
     *
     * - Optionally show a summary like “3 notifications”.
     */
    return <div>{/* TODO */}</div>
}
