export default function Ex2MoodTracker() {
    /**
     * TODOs:
     *
     * - Store the current mood in state ("happy", "neutral", etc.).
     *   (See: State & Events.)
     *
     * - Provide controls (buttons/select) to change the mood.
     *
     * - Conditionally render different emojis or messages based on mood.
     *   (See: Conditional Rendering.)
     *
     * - Create a <MoodDisplay> child component that receives mood
     *   via props and displays the UI for that mood.
     *
     * - (Optional) Keep a history of previous moods in an array and
     *   render it with .map().
     */
    return <div>{/* TODO */}</div>
}
