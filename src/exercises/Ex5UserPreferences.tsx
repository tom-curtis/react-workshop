export default function Ex5UserPreferences() {
    /**
     * TODOs:
     *
     * - Store user preferences (e.g. theme, fontSize, showLineNumbers)
     *   in state.
     *
     * - Provide controls for updating each preference.
     *
     * - Use useEffect to save preferences to localStorage whenever
     *   they change.
     *   (See: useEffect & Side Effects.)
     *
     * - On mount, load saved preferences from localStorage.
     *
     * - Use conditional rendering / dynamic styles to update a
     *   preview area based on the current preferences.
     */
    return <div>{/* TODO */}</div>
}
