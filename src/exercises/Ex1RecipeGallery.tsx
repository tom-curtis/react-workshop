export default function Ex1RecipeGallery() {
    /**
     * TODOs:
     *
     * - Create a reusable <RecipeCard> component with props like
     *   title, difficulty, cookingTime, tags, description.
     *   (See: Components & JSX.)
     *
     * - Build an array of recipe objects (or use a provided one).
     *
     * - Render a list of <RecipeCard> components using .map().
     *   (See: Lists & Derived State.)
     *
     * - Add a difficulty filter control (buttons or a select).
     *
     * - Derive a filtered list from the recipes array rather than
     *   storing filtered results in state.
     *
     * - Use conditional rendering to show “No recipes found”.
     */
    return <div>{/* TODO */}</div>
}
