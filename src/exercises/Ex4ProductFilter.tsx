export default function Ex4ProductFilter() {
    /**
     * TODOs:
     *
     * - Create an array of product objects:
     *   id, name, category, price, inStock.
     *
     * - Render all products using .map() with proper keys.
     *   (See: Lists & Derived State.)
     *
     * - Add filter controls:
     *   - text search
     *   - category filter
     *   - “in stock only” toggle
     *
     * - Compute filteredProducts using .filter()
     *   based on the current search/filter state.
     *
     * - Show “X of Y items displayed”.
     *
     * - Conditionally show a “No matching products” message.
     */
    return <div>{/* TODO */}</div>
}
